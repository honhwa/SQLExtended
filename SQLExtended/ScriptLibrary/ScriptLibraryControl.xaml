<UserControl x:Class="SQLExtended.ScriptLibrary.ScriptLibraryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
             Background="#1E1E1E">

    <UserControl.Resources>
        <!-- Dark theme button matching the Search / History tool windows -->
        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3F3F46"/>
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#666666"/>
                                <Setter Property="BorderBrush" Value="#3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Script list item -->
        <Style x:Key="ScriptItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#D4D4D4"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A2D2E"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#094771"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Search bar + New -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox"
                     Grid.Column="0"
                     Background="#333337"
                     Foreground="#D4D4D4"
                     CaretBrush="#D4D4D4"
                     BorderBrush="#555555"
                     BorderThickness="1"
                     Padding="6,4"
                     FontSize="13"
                     VerticalContentAlignment="Center"
                     KeyDown="SearchTextBox_KeyDown"
                     TextChanged="SearchTextBox_TextChanged"/>

            <Button Grid.Column="1"
                    Content="New"
                    Style="{StaticResource ToolButton}"
                    Click="New_Click"
                    Margin="4,0,0,0"
                    ToolTip="Create a new user script"/>
        </Grid>

        <!-- Row 1: split — script list (left) / preview + actions (right) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" MinWidth="160"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="200"/>
            </Grid.ColumnDefinitions>

            <!-- Grouped script list -->
            <ListBox x:Name="ScriptList"
                     Grid.Column="0"
                     Background="#1E1E1E"
                     BorderBrush="#333337"
                     BorderThickness="1"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ItemContainerStyle="{StaticResource ScriptItem}"
                     SelectionChanged="ScriptList_SelectionChanged"
                     MouseDoubleClick="ScriptList_MouseDoubleClick">
                <ListBox.ContextMenu>
                    <ContextMenu Background="#2D2D30" Foreground="#D4D4D4" BorderBrush="#555555">
                        <MenuItem Header="Open in New Query Window" Click="Open_Click"/>
                        <MenuItem Header="Run" Click="Run_Click"/>
                        <MenuItem Header="Copy to Clipboard" Click="Copy_Click"/>
                        <Separator/>
                        <MenuItem Header="Edit..." Click="Edit_Click"/>
                        <MenuItem Header="Duplicate as User Script" Click="Duplicate_Click"/>
                        <MenuItem Header="Delete" Click="Delete_Click"/>
                    </ContextMenu>
                </ListBox.ContextMenu>
                <ListBox.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <Border Background="#252526" Padding="6,3" Margin="0,2,0,0">
                                    <TextBlock Text="{Binding Name}" Foreground="#6A9955"
                                               FontSize="11" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ListBox.GroupStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#x1F512;" FontSize="11" Margin="0,0,4,0"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding BuiltInBadgeVisibility}"
                                       ToolTip="Built-in (read-only)"/>
                            <TextBlock Text="{Binding Name}" FontSize="13" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center"
                          Background="#2D2D30" Cursor="SizeWE"
                          ResizeBehavior="PreviousAndNext" ShowsPreview="True"/>

            <!-- Preview + actions -->
            <Grid Grid.Column="2" Margin="4,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="PreviewHeader" Grid.Row="0"
                           Text="Select a script"
                           Foreground="#D4D4D4" FontSize="13" FontWeight="SemiBold"
                           Padding="2,2,2,2"/>

                <TextBlock x:Name="PreviewDescription" Grid.Row="1"
                           Foreground="#808080" FontSize="11"
                           TextWrapping="Wrap" Margin="2,0,2,4"/>

                <avalonedit:TextEditor x:Name="PreviewEditor" Grid.Row="2"
                         IsReadOnly="True"
                         ShowLineNumbers="True"
                         WordWrap="False"
                         FontFamily="Cascadia Code, Consolas, Courier New"
                         FontSize="12"
                         Background="#1E1E1E"
                         Foreground="#D4D4D4"
                         LineNumbersForeground="#555555"
                         BorderBrush="#333337"
                         BorderThickness="1"
                         Padding="4"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"/>

                <!-- Action buttons -->
                <WrapPanel Grid.Row="3" Margin="0,6,0,0">
                    <Button x:Name="OpenButton" Content="Open" Style="{StaticResource ToolButton}"
                            Click="Open_Click" ToolTip="Open in a new query window"/>
                    <Button x:Name="RunButton" Content="Run" Style="{StaticResource ToolButton}"
                            Click="Run_Click" ToolTip="Open in a new query window and execute"/>
                    <Button x:Name="CopyButton" Content="Copy" Style="{StaticResource ToolButton}"
                            Click="Copy_Click" ToolTip="Copy script to clipboard"/>
                    <Border Width="1" Background="#3A3A3A" Margin="4,2,8,2"/>
                    <Button x:Name="EditButton" Content="Edit" Style="{StaticResource ToolButton}"
                            Click="Edit_Click" ToolTip="Edit this user script"/>
                    <Button x:Name="DeleteButton" Content="Delete" Style="{StaticResource ToolButton}"
                            Click="Delete_Click" ToolTip="Delete this user script"/>
                </WrapPanel>
            </Grid>
        </Grid>

        <!-- Row 2: status bar -->
        <TextBlock x:Name="StatusText" Grid.Row="2"
                   Text="Ready"
                   Foreground="#808080" FontSize="11"
                   Margin="0,4,0,0" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
